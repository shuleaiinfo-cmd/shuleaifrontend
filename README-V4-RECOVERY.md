# Shule AI Frontend V4 Recovery

This frontend is mostly the same as V3 but with safer behavior around class-teacher assignment verification and less intrusive debug popups.

The main fix is in the backend/database. Deploy backend V4 first.